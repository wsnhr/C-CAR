# C-CAR 项目代码导读（C 初学者版）

这是一套使用 C 语言、EasyX 和 Windows API 编写的车辆保险管理程序。程序把数据保存在内存中的 `AppContext` 结构体里，并通过文本文件实现持久化。

## 1. 推荐阅读顺序

1. `main.c`：程序入口，了解程序如何启动。
2. `app_types.h`：认识用户、车辆、保单、理赔和总上下文。
3. `user.h`、`car.h`、`insurance.h`、`storage.h`：按模块阅读公开接口。
4. `app_utils.h/.c`：理解日期、字符串复制和输入验证的公共实现。
5. `password_hash.h/.c`：理解普通用户密码如何加盐、哈希和验证。
6. `gui.c`：理解窗口初始化、数据加载和事件循环。
7. `gui_home.c`、`gui_car.c`、`gui_insurance.c`：理解一次按钮点击如何触发业务操作。
8. `user.c`、`car.c`、`insurance.c`：理解真正的数据查询、增加、修改、删除和权限判断。
9. `storage.c`：理解内存数据如何写入文本文件、下次运行时又如何读回。

## 2. 模块关系

```text
main.c（程序入口）
  └─ gui_run(&ctx)
      ├─ storage.c：启动时加载 users/cars/policies/claims
      ├─ gui_home.c：注册、登录、退出、切换页面
      ├─ gui_car.c：车辆页面的显示和操作
      ├─ gui_insurance.c：保单/理赔页面的显示和操作
      ├─ car.c、user.c、insurance.c：业务规则和内存数据操作
      ├─ password_hash.c：随机盐、SHA-256 和密码验证
      └─ storage.c：操作成功后保存数据、记录日志
```

`gui_*.c` 负责“向用户展示什么、用户点了什么”；`car.c`、`user.c`、`insurance.c` 负责“这个操作是否允许、怎样修改数据”；`storage.c` 负责“怎样把结果永久保存”。这种分工可以避免把界面、规则和文件读写全部塞进一个文件。

## 3. 核心数据关系

```text
User（用户）
  ├─ 实名凭据（姓名 + 身份证号的加盐哈希）
  └─ owner 用户名
      └─ Car（车辆）
          └─ plate 车牌号
              └─ Policy（保单）
                  └─ policy_id 保单号
                      └─ Claim（理赔）
```

以上关系没有使用数据库外键，而是通过字符串字段手工关联。因此删除用户或车辆时，程序必须主动删除相关车辆、保单和理赔，这叫“级联删除”。

## 4. AppContext 为什么重要

`AppContext` 是程序运行期间的总数据容器，其中包含用户数组、车辆数组、保单数组、理赔数组、各数组当前有效元素数量，以及当前登录用户名。大多数函数接收 `AppContext* ctx`，也就是接收这个容器的地址。函数拿到地址后可以直接读取或修改同一个容器，而不必复制整份数据。

```c
AppContext ctx;  /* 创建一个实际对象 */
init_app(&ctx);  /* &ctx 取得对象地址并传给函数 */
```

当参数写成 `const AppContext* ctx` 时，表示函数通过指针读取数据，但承诺不修改它；写成 `AppContext* ctx` 时，则允许修改。

## 5. `.h` 和 `.c` 的区别

- `.h` 头文件主要放类型定义、宏和函数声明，相当于模块的“使用说明书”。
- `.c` 源文件放函数实现，相当于模块内部真正执行的代码。
- `#include "app_types.h"` 获得数据类型；包含 `car.h` 等模块头文件获得对应函数声明。
- `common.h` 现在只作为兼容聚合入口，新代码应优先包含真正使用的模块头文件。
- 函数声明末尾有分号，例如 `int save_cars(const AppContext* ctx);`；函数定义后面跟函数体 `{ ... }`。

## 6. 常见 C 语法速查

- `Car* p`：`p` 是一个指向 `Car` 的指针。
- `&car`：取得变量 `car` 的地址。
- `*p`：访问指针所指向的对象。
- `p->plate`：通过结构体指针访问成员，等价于 `(*p).plate`。
- `car.plate`：通过结构体对象本身访问成员。
- `const char* text`：指向字符的指针，函数不应通过它修改字符串内容。
- `char name[20]`：20 个字符单元的数组；作为 C 字符串使用时还要为结尾的 `\0` 留一个位置。
- `static` 函数：只允许当前 `.c` 文件使用，属于模块内部工具。
- `typedef`：为一个类型取更易用的名字。
- `enum`：为一组整数常量取有意义的名称。
- `NULL`：空指针，通常表示“没有找到对象”或“文件打开失败”。
- `{0}`：把结构体或数组的所有成员初始化为零。

## 7. 一次操作的数据流

以“添加车辆”为例：

1. `gui_car.c` 显示输入框并收集车牌、品牌等信息。
2. 输入验证函数检查字符串长度、日期和价格。
3. `add_car_for_current_user` 判断登录状态、管理员权限和实际车主。
4. `add_car` 检查容量与重复车牌，然后把新车辆写入 `ctx->cars`。
5. GUI 根据返回值显示成功或失败消息。
6. 成功后调用 `save_cars` 把内存中的车辆数组写入 `cars.txt`。
7. `append_operation_log` 把操作写入 `operations.log`。

其他增删改操作也遵循“界面收集输入 → 业务层校验和修改 → 文件层保存”的路径。

## 8. 阅读时需要注意

- 数组容量由 `MAX_USERS` 等宏固定，添加前必须检查计数是否到达上限。
- C 字符串没有自动边界检查，复制字符串时必须关注目标数组大小。
- 表示“成功或失败”的接口已统一为 `1=成功、0=失败`；`date_compare` 等比较函数会按自身注释返回负数、零或正数。
- 数据文件使用相对路径，实际读写位置取决于程序启动时的工作目录。
- `admin` 是特殊用户名，相关权限判断集中在认证和面向当前用户的包装函数中。
- 普通用户注册时必须填写姓名和身份证号；身份证号会校验长度、出生日期和校验码。
- `users.txt` 只保存密码与实名组合凭据各自的盐和 SHA-256 哈希，不保存明文密码、姓名或身份证号。
- 忘记密码时，用户通过注册时的姓名和身份证号验证身份，再设置新密码；内置管理员密码不能通过该流程重置。
- 理赔必须经过“待审核 → 已通过 → 已结案”流程；只有管理员可以审核和结案。

## 9. 重构后怎样判断应该调用哪个接口

- GUI 发起增删改时，优先调用名称以 `_for_current_user` 结尾的接口。这些接口会统一检查登录状态、管理员身份和数据所有权。
- 只供某个 `.c` 文件内部使用的查找、删除辅助函数声明为 `static`，不会暴露给其他模块，也减少了误用入口。
- 日期、字符串复制和通用输入验证集中在 `app_utils.c`；业务文件不再各自保存一份重复实现。
- 删除用户、车辆或保单时，由业务层完成级联删除；GUI 只负责在成功后调用 `save_*` 持久化结果。
- `tests/refactor_smoke_test.c` 不启动 GUI，专门验证实名注册、密码找回、权限、级联删除和保存/加载逻辑。
