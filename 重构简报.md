# C-CAR 项目重构简报

## 一、重构目标

本次重构主要解决以下问题：

- 公共接口集中在单个头文件中，模块职责不清晰。
- 日期、字符串和输入校验存在多份重复实现。
- GUI 层可以直接调用底层修改函数，权限检查不够集中。
- 删除用户、车辆和保单时，关联数据的清理逻辑分散。
- 部分函数重复、未使用或返回值语义不一致。
- 持久化存在用户删除后可能重新出现、旧车辆格式读取不稳定等问题。

## 二、核心改动

### 1. 拆分公共头文件

原来的 `common.h` 同时包含数据类型和多个模块的函数声明。重构后拆分为：

| 文件 | 职责 |
| --- | --- |
| `app_types.h` | 定义用户、车辆、保单、理赔、日期和 `AppContext` |
| `app_utils.h/.c` | 日期、字符串复制和通用输入校验 |
| `password_hash.h/.c` | 安全随机盐、SHA-256 哈希和密码验证 |
| `user.h/.c` | 注册、登录、登出和用户删除 |
| `car.h/.c` | 车辆查询、增加、修改、删除和权限判断 |
| `insurance.h/.c` | 保单、理赔、保费与赔付计算 |
| `storage.h/.c` | 文本文件加载、保存和操作日志 |

`common.h` 仅作为兼容聚合头文件保留，新代码直接包含实际需要的模块头文件。

### 2. 合并重复工具逻辑

新建 `app_utils.c`，统一实现：

- `date_today`：获取当天日期。
- `date_compare`：比较两个日期。
- `date_days_in_month`：计算某年某月的天数，包含闰年规则。
- `date_is_valid`：校验日期是否合法。
- `copy_text`：安全复制 C 字符串并保证以 `\0` 结尾。
- `validate_plate`、`validate_string_len`、`validate_price_value`：统一输入校验。

车辆、保险和 GUI 模块不再各自保存重复版本。

### 3. 收口业务接口

- GUI 对数据进行增删改时，统一调用以 `_for_current_user` 结尾的权限安全接口。
- 登录状态由 `app_is_logged_in` 判断，管理员身份由 `app_is_admin` 判断。
- 只供模块内部使用的查找和删除函数改为 `static`，减少外部可见接口。
- 表示成功或失败的业务函数统一使用 `1=成功、0=失败`。

### 4. 集中级联删除

数据关系为：

```text
User -> Car -> Policy -> Claim
```

重构后：

- 删除车辆时，同时删除该车辆的保单和理赔。
- 删除保单时，同时删除引用该保单的理赔。
- 删除用户时，统一清理其车辆、保单和理赔。
- 持久化模块不再负责业务关系判断。

### 5. 修正持久化逻辑

- `save_users` 改为全量重写 `users.txt`，避免已删除用户在重启后重新出现。
- `load_cars` 改为 `fgets + sscanf` 逐行解析。
- 新车辆文件的 9 字段格式和旧文件的 4 字段格式均可读取。
- 所有 `save_*` 和 `load_*` 接口补充空指针保护。
- `file.c` 更名为职责更明确的 `storage.c`。

### 6. 消除 GUI 重复代码

- 首页、车辆页和保险页的按钮布局改为共享常量数组。
- 绘制和鼠标命中检测使用同一份按钮定义。
- 车辆、保单和理赔列表使用统一的 `PageRange` 分页计算。
- 保险产品改为 `InsuranceProduct` 结构体数组，取代多个必须保持下标一致的平行数组。
- 创建保单时使用 `PolicyTerms` 传递一组关联参数，GUI 不再在创建后直接覆盖保单字段。

### 7. 同步上游功能

- 普通用户密码改为“16 字节随机盐 + SHA-256”，`users.txt` 不再保存明文。
- 注册增加姓名和身份证校验，实名组合凭据加盐哈希后保存；找回密码需验证注册时的姓名和身份证号。
- 哈希实现从巨大的 `user.c` 分离为 `password_hash.c/.h`，保持用户业务代码清晰。
- 理赔状态扩展为待审核、已通过、已结案和已驳回。
- 普通用户只能提交理赔；管理员先审核，只能结案已审核通过的理赔。

## 三、删除的冗余内容

本次删除或替换了以下类型的冗余内容：

- 重复的登录和管理员判断函数。
- 多份“获取今天日期”和“比较日期”函数。
- 未使用的控制台注册、登录和列表函数。
- 未使用的输入、查找和保单修改函数。
- 不必要的可见车辆数组复制函数。
- 持久化模块中重复的级联删除逻辑。

## 四、重构后的调用路径

以“删除车辆”为例：

```text
用户点击删除
  -> gui_car.c 收集车牌
  -> remove_car_for_current_user 检查登录和所有权
  -> remove_car 删除车辆
  -> remove_insurance_for_plate 删除关联保单和理赔
  -> GUI 调用 save_cars/save_policies/save_claims
  -> append_operation_log 记录操作
```

这种路径将界面、权限、业务规则和文件保存分层，便于阅读、测试和后续修改。

## 五、验证结果

- Visual Studio 解决方案完整重建成功。
- 129 个函数均重新编译，没有编译错误或警告。
- 业务层独立测试使用 `/W4 /WX` 编译通过。
- 用户和车牌查重测试通过。
- 密码加盐哈希、错误密码拒绝和哈希记录重新加载测试通过。
- 理赔审核权限、状态转换和结案顺序测试通过。
- 普通用户越权操作阻止测试通过。
- 车辆和用户级联删除测试通过。
- 用户全量保存、业务数据加载以及旧车辆文件兼容测试通过。
- 21 个 C 源文件和头文件均保持 UTF-8 BOM，中文注释可正常显示。

## 六、对学习和维护的影响

- 初学者可以先阅读 `app_types.h`，再阅读各模块头文件，不需要一次理解所有函数。
- 函数声明和实现按业务领域分类，更容易找到代码位置。
- 权限判断和级联删除只需在业务层维护一份。
- 公共工具修改后，所有调用模块自动获得同一行为。
- 新增功能时可以继续沿用“GUI 收集输入 → 业务层校验和修改 → 存储层保存”的结构。

## 七、后续建议

1. 将普通用户的单次 SHA-256 升级为 PBKDF2、scrypt 或 Argon2 等专用密码派生算法。
2. 将文件保存改为“先写临时文件，成功后替换”，降低写入中断造成文件损坏的风险。
3. 为各个业务接口增加更细的错误码，便于 GUI 区分“重复”、“越权”和“容量已满”。
4. 继续扩展 `tests/refactor_smoke_test.c`，覆盖容量上限、损坏数据行和文件写入失败等边界情况。

---

重构后的详细阅读顺序、C 语法说明和数据关系请参阅《项目代码导读.md》。
